version: '3.9'
services:
  gateway:
    build: .
    container_name: nonogram_gateway
    ports:
      - "3333:3333"
    environment:
      PORT: 3333
      HOST: 0.0.0.0
      ALLOWED_ORIGINS: http://localhost:3000,http://192.168.1.78:3000
